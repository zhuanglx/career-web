<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
						
<mapper namespace="com.railway.labor.career.mapper.UserMapper">
	<resultMap id="LoginResultMap"
		type="com.railway.labor.career.model.dto.LoginInfoDTO">
		<id column="id" property="id" jdbcType="BIGINT" />
		<result column="user_no" property="userNo" jdbcType="VARCHAR" />
		<result column="user_name" property="userName" jdbcType="VARCHAR" />
		<result column="password" property="password" jdbcType="VARCHAR" />
	</resultMap>
	<resultMap id="BaseResultMap" type="com.railway.labor.career.model.dto.UserDTO">
		<id column="id" property="id" jdbcType="BIGINT" />
		<result column="user_no" property="userName" jdbcType="VARCHAR" />
		<result column="user_name" property="userName" jdbcType="VARCHAR" />
		<result column="password" property="password" jdbcType="VARCHAR" />
		<result column="creator" property="creator" jdbcType="VARCHAR" />
		<result column="create_date" property="createDate" jdbcType="DATE" />
		<result column="modifier" property="modifier" jdbcType="VARCHAR" />
		<result column="modify_date" property="modifyDate" jdbcType="DATE" />
		<result column="remarks" property="remarks" jdbcType="VARCHAR" />
		<result column="del_flag" property="delFlag" jdbcType="VARCHAR" />
	</resultMap>
	<sql id="Base_Column_List">
		id,user_no,user_name,password,creator, create_date, modifier, modify_date, remarks, del_flag
	</sql>

<select id="login" resultMap="LoginResultMap">
		SELECT
		<include refid="Base_Column_List" />
		FROM t_user 
		WHERE user_no = #{userNo, jdbcType=VARCHAR} and password = #{password, jdbcType=VARCHAR} and del_flag = '0'
	</select>
	
	<select id="query" resultMap="BaseResultMap"  parameterType="com.railway.labor.career.model.query.UserQuery">
		SELECT
		<include refid="Base_Column_List" />
		FROM t_user  and del_flag = '0'
	</select>

	<select id="get" parameterType="java.lang.Long" resultMap="BaseResultMap">
		SELECT
		<include refid="Base_Column_List" />
		FROM t_user
		WHERE id = #{id}  and del_flag = '0'
	</select>

	<insert id="insert" parameterType="com.railway.labor.career.model.dto.UserDTO">
		INSERT INTO
		t_user
		(user_no,user_name,password,creator, create_date, modifier, modify_date, remarks, del_flag)
		VALUES
		(#{userNo}, #{userName}, '11', #{creator}, #{createDate},#{modifier}, #{modifyDate},#{remarks},#{delFlag})
	</insert>

	<update id="update" parameterType="com.railway.labor.career.model.dto.UserDTO">
		UPDATE
		t_user
		SET
		userName = #{userName}, password = #{password}
		WHERE
		id = #{id}  and del_flag = '0'
	</update>

	<delete id="delete" parameterType="java.lang.Long">
		UPDATE
		t_user
		SET 
		del_flag = '1'
		WHERE
		id =#{id} and del_flag = '0'
	</delete>

</mapper>